#include <stdio.h>

int main() {
    int n, sum = 0;

    printf("Enter a number (n): ");
    scanf("%d", &n);

    for (int i = 1; i <= n; i += 2) {
        sum += i * i;
    }

    printf("Sum of the squares of odd numbers: %d\n", sum);

    return 0;
}
